# Hanji

Tiny desktop memo app.

## Shortcut

- `Ctrl + Q`: toggle `Always on Top`

## Platform notes

- `Always on Top` uses X11 and is intended for X11 or XWayland. Native Wayland is not supported.
- Korean input assumes fcitx5. When `XMODIFIERS` is unset, Hanji sets it to `@im=fcitx`.
